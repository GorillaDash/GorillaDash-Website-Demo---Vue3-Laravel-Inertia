# Sends http visitors to https at the load balancer. See ingress.yaml.
apiVersion: networking.gke.io/v1beta1
kind: FrontendConfig
metadata:
  name: hungry-gorilla-frontend
spec:
  redirectToHttps:
    enabled: true
    responseCodeName: MOVED_PERMANENTLY_DEFAULT
